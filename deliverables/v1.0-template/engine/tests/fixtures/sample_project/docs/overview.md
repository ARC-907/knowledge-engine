# Overview

This sample project demonstrates how the Knowledge-Engine project-docs scanner
discovers and ingests documentation. It is intentionally small and generic.

## Architecture

The widget service reads requests, validates them, and returns a result. See
`src/widget.py` for the reference implementation.
